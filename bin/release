#!/usr/bin/env bash
# bin/release <build-dir>

# default_process_types:
#   console: bin/fakesu

cat <<EOF
---
config_vars:
  PATH: bin:node_modules/.bin:/usr/local/bin:/usr/bin:/bin:/app/bin
  DISPLAY: localhost:1
EOF
